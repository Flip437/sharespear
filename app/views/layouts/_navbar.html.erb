<nav class="navbar navbar-expand-lg bg-white sticky-top">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <%= link_to root_path do%>
        <span class="navbar-brand">Sharespeare</span>
        <% end %>
      </li>
      
      
      
      
<!-- SI UTILISATEUR CONNECTÉ, ON AFFICHE UNE DROPDOWN -->
      <% if current_user %>
        <li class="nav-item active">
          <%= link_to "Dashboard", user_dashboard_index_path(current_user.id), class: "nav-link" %>
          <span class="sr-only">(current)</span>
        </li>
        <li class="nav-item active">
          <%= link_to "Profil", user_path(current_user.id), class: "nav-link" %><span class="sr-only">(current)</span>
        </li>
        <li class="nav-item dropdown">
          <%= link_to "Edit / Sign Out", user_path(current_user.id), class: "nav-link", class: "nav-link dropdown-toggle", href: "#", id: "navbarDropdownMenuLink", data: { toggle: "dropdown" }, aria: { haspopup: true, expanded: false } %><span class="sr-only">(current)</span>
          <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <%= link_to edit_user_path(current_user.id) do %>
            <span class="dropdown-item">Edit Profil</span>
            <% end %>
            <%= link_to destroy_user_session_path, method: :delete do %>
            <span class="dropdown-item">Sign Out</span>
            <% end %>
          </div>
        </li>
        
        
<!-- SI UTILISATEUR NON CONNECTÉ, ON AFFICHE UNE AUTRE DROPDOWN -->
        <% else %>
          <li class="nav-item dropdown">
            <%= link_to "Sign In / Sign Up", root_path, class: "nav-link", class: "nav-link dropdown-toggle", href: "#", id: "navbarDropdownMenuLink", data: { toggle: "dropdown" }, aria: { haspopup: true, expanded: false } 
            
            
            %><span class="sr-only">(current)</span>
            <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
              
              <%= link_to new_user_session_path do %>
              <span class="nav-link">Sign In</span>
              <% end %>
              
              <%= link_to new_user_registration_path do %>
              <span class="nav-link">Sign Up</span>
              <% end %>
            </div>
          </li>
      <% end %>
    </ul>
    
  </div>
</nav>


















