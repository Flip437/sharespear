<ul class="nav nav-pills nav-pills-danger justify-content-center" role="tablist">
	<li class="nav-item">
		<%= link_to "Mes Amis", home_index_path(selector: { filter: "friends" }), class: "nav-link", style: "color: orange;", remote:true %>
	</li>
	<li class="nav-item">
		<%= link_to "Tout genre",  home_index_path(selector: { filter: "all" }), class: "nav-link", remote:true %>
	</li>

	<% shuffled_categories = @categories.shuffle %>
	<% shuffled_categories[0, @displayed_categories_count].each do |category| %>
		<li class="nav-item">
			<%= link_to category, home_index_path(selector: { categories: category }), class: "nav-link", remote:true %>
		</li>
	<% end %>

	<li class="dropdown">
		<button class="btn btn-secondary btn-round dropdown-toggle" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" style="margin: 0px; padding: 1.1em;">
			Autres Catégories
		</button>
		<% shuffled_categories[@displayed_categories_count..-1].each do |category| %>
			<div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
				<%= link_to category, home_index_path(selector: { categories: category }), class: "dropdown-item", remote:true %>
			</div>
		<% end %>
		</li>
</ul>
